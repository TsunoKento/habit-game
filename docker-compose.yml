services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_DSN=/data/habit.db
    restart: unless-stopped

  dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    working_dir: /src
    volumes:
      - .:/src
      - ./data:/data
    environment:
      - DATABASE_DSN=/data/habit.db
    profiles:
      - dev

  watch:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    working_dir: /src
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - .:/src
      - ./data:/data
    environment:
      - DATABASE_DSN=/data/habit.db
    profiles:
      - dev
